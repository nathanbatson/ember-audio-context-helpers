
import { audioProgress } from 'dummy/helpers/audio-progress';
import { module, test } from 'qunit';

module('Unit | Helper | audio progress');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = audioProgress([42]);
  assert.ok(result);
});

