
import { audioTransition } from 'dummy/helpers/audio-transition';
import { module, test } from 'qunit';

module('Unit | Helper | audio transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = audioTransition([42]);
  assert.ok(result);
});

